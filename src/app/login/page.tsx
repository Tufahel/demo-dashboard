import LoginComponent from '@/components/login/login';
import React from 'react';

const LoginPage = () => {
    return (
        <div>
            <LoginComponent />
        </div>
    );
};

export default LoginPage;